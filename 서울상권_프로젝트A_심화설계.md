# 프로젝트 A 심화 설계
## 점포 생존 예측 코어 + 창업 입지추천 래퍼

> **이전 문서와의 차이**: 아이디어 나열 → 실행 설계로 전환.
> 라벨 정의, 누수 차단, 평가 프로토콜 등 "실제로 만들 때 프로젝트를 망치는 지점"에 분량을 집중했습니다.

---

# 0. 방향 확정 사항

| 항목 | 결정 | 근거 |
|---|---|---|
| **메인 프로젝트** | A (점포 생존) | 라벨이 객관적이고 평가 방어력이 강함 |
| **구조** | 예측이 코어, 추천은 래퍼 | 추천엔 ground truth가 없음. 추천하려면 예측이 선행 필수 |
| **시계열 의존도** | 낮음 | 횡단면 지도학습 + 생존분석. 시계열 모델링 불필요 |
| **차별화 축** | 생존분석 + 프랜차이즈 비교 | 부트캠프 포트폴리오에서 희소 |
| **프로젝트 B** | 보류 | 조기경보는 시간축 필수. 여력 남으면 확장 |

**한 줄 정의**
> "서울에서 특정 업종으로 창업할 때, 상권 특성이 점포 생존에 얼마나 영향을 주는지 정량화하고, 이를 근거로 입지를 추천한다."

---

# 1. 분석 단위와 라벨 설계 — 프로젝트의 성패 지점

여기를 대충 넘기면 뒤의 모든 모델링이 무의미해집니다. 가장 먼저, 가장 신중하게 확정해야 할 부분입니다.

## 1-1. 분석 단위

**점포(사업장) 1건 = 1 row**

상권×업종 집계 단위가 아니라 점포 단위로 갑니다. 이유:
- LOCALDATA 인허가가 점포 단위 개업일/폐업일을 주므로 라벨 해상도를 낮출 이유가 없음
- 생존분석은 개체 단위 duration이 필요
- 샘플 수가 수만~수십만으로 확보되어 모델이 배울 여지가 큼

## 1-2. 코호트 설계 — 두 가지 방식과 트레이드오프

### 방식 A: 고정시점 코호트 (Fixed-time cohort)

기준시점 `T0`를 정하고, **T0에 영업 중인 모든 점포**를 대상으로 이후 관찰 윈도우에서 폐업 여부를 라벨링.

```
T0 = 2021-01-01 (예시)
대상 = T0 이전 개업 & T0 시점 영업중인 점포
라벨 = [T0, T0+24개월] 내 폐업하면 1, 아니면 0
피처 = T0 이전 정보만
```

- 장점: 샘플이 많고 구성이 단순
- **문제: 좌측절단(left truncation)**. T0 시점에 개업 3개월 차 점포와 10년 차 점포가 한 테이블에 섞임. 생존 위험이 완전히 다른데 같은 취급을 받음
- 완화: `T0 시점 영업기간(개월)`을 반드시 피처로 포함. 그래도 생존분석 관점에선 편향이 남음

### 방식 B: 개업 코호트 (Inception cohort) ★권장

**특정 기간에 신규 개업한 점포만** 모아 개업일을 시작점(t=0)으로 정렬.

```
대상 = 2019Q1 ~ 2021Q4 개업 점포
t=0  = 각 점포의 개업일
event = 폐업 (관측되면 1)
duration = 개업일 → 폐업일 또는 데이터 종료일까지의 개월 수
censoring = 데이터 종료 시점까지 생존 시 우측 절단(1이 아님)
```

- **생존분석에 이상적**: 모든 개체가 동일한 t=0을 가짐 → Kaplan-Meier 곡선이 해석 가능해짐
- 프로젝트 질문("**신규 창업자**가 살아남는가")과 대상이 정확히 일치
- 단점: 샘플 수가 줄어듦. 대신 서울 전체 요식업 기준이면 충분히 확보될 가능성이 높음

> **권장**: 방식 B를 메인으로. 이진분류 버전은 "개업 후 24개월 내 폐업 여부"로 방식 B에서 파생시키면 됩니다. 두 트랙이 같은 코호트를 공유하게 되어 결과 비교가 깔끔해집니다.

## 1-3. 라벨 정의 시 반드시 확인할 것

| 확인 사항 | 왜 중요한가 |
|---|---|
| 인허가 상태 코드 체계 | "폐업" 외에 휴업/취소/말소 등 여러 상태가 존재. 무엇을 event로 볼지 명시적으로 정의 |
| 업종 변경 vs 폐업 | 같은 자리에서 업태만 바꾼 경우 진짜 폐업인가? 정의를 문서화해야 함 |
| 양도·양수 | 대표자 변경이 폐업으로 기록되는지 확인 필요. 아니라면 생존 과대추정 |
| 우측 절단 처리 | 데이터 종료 시점 생존 = 폐업 0이 아니라 **censored**. 이진분류에선 관찰 윈도우를 충분히 확보해 완화 |
| 관찰 윈도우 길이 | 24개월 권장. 너무 짧으면 event가 부족하고, 너무 길면 최근 코호트를 못 씀 |

---

# 2. 데이터 파이프라인 설계

## 2-1. 데이터 소스와 역할

| 소스 | 역할 | 조인 키 |
|---|---|---|
| **LOCALDATA 인허가** | 라벨 (개업일/폐업일), 점포 좌표·업태 | 좌표 → 상권 |
| **소상공인 상가정보** | 상호명(브랜드 식별), 업종 세분류, 좌표 | 좌표 / 상호명 |
| **상권분석서비스 5종** | 상권 단위 피처 (매출·유동인구·상주인구·집객시설·점포) | 상권코드 |
| **상권영역 SHP** | 공간조인 기준, 지도 시각화 | 폴리곤 |
| **공정위 가맹사업정보** | 브랜드 단위 평균매출·개폐점 수 (보조) | 브랜드명 |
| 국토부 실거래가(상업용) | 임대료 대리변수 (선택) | 지역 |

## 2-2. 조인 전략

```
[LOCALDATA 점포] --좌표--> [상권 폴리곤 공간조인] --> 상권코드 부여
                     |
                     +--상호명 퍼지매칭--> [브랜드 플래그]
                     |
                     +--반경 버퍼--> [경쟁밀도 계산]

[상권코드] --> [상권분석서비스 5종 병합, T0 이전 시점만] --> 상권 피처
```

**CRS 통일이 첫 번째 관문**입니다. SHP는 EPSG:5179/5181, 위경도는 4326. 공간조인 전에 반드시 한쪽으로 통일하고, 샘플 몇 개를 지도에 찍어 육안 검증하세요. 여기서 틀리면 이후 전부 무의미해집니다.

## 2-3. 상권 경계 밖 점포 처리

상권 폴리곤은 서울 전체를 덮지 않습니다. 폴리곤 밖 점포는:
- (a) 제외 → 샘플 손실, 선택편향 가능성
- (b) 최근접 상권에 할당 → 노이즈 유입
- (c) 별도 "상권외" 범주로 유지 → 대조군으로 활용 가능 (권장)

어느 쪽이든 **비율을 보고하고 선택 이유를 문서화**하는 게 중요합니다.

---

# 3. 피처 엔지니어링 — 누수 안전 설계

## 3-1. 절대 원칙

> **모든 피처는 각 점포의 개업일(t=0) 시점에 알 수 있었던 정보만 사용한다.**

창업자가 입지를 고를 때 실제로 참조 가능했던 정보만 쓰는 것 — 이게 이 프로젝트의 실용성과 방법론적 엄밀성을 동시에 지킵니다.

## 3-2. 피처 그룹

### (1) 상권 규모·수요
- 유동인구 총량, 상주인구, 직장인구
- 집객시설 수 (학교/병원/역/관공서)
- 지하철역까지 거리, 반경 내 역 개수

### (2) 시간대·요일 프로파일 ★핵심
- 시간대별 유동인구 **비율** (총량 아닌 구성비 → 상권 성격 포착)
- 주중/주말 비율 → "주말 공동화" 지표
- 이 프로파일이 클러스터링의 주 재료이자 PCA 해석의 핵심 축

### (3) 경쟁 구조
- 반경 300m/500m 내 **동일 업종** 점포 수
- 반경 내 전체 점포 수 대비 동일 업종 비율 (업종 집중도)
- 최근접 동일업종 점포까지 거리
- **자기 자신 제외(self-exclusion) 필수** — 자기를 포함하면 미묘한 누수

### (4) 상권 성숙도 (t=0 이전 정보만)
- 개업 직전 4~8분기 상권 매출 추세 (기울기, 변화율)
- 직전 기간 신규 개업 수 / 폐업 수 → 상권 회전율
- 상권 내 점포 평균 영업기간

### (5) 점포 고유 속성
- 업종 세분류
- **프랜차이즈 여부** (상호명 매칭 기반) ★차별화 피처
- 개업 시점 (연도·분기 → 코로나 등 거시충격 통제)

### (6) 파생: 상권 유형 클러스터
- KMeans/DBSCAN 결과를 범주형 피처로 투입
- **주의**: 클러스터링을 전체 기간 데이터로 fit하면 미래 정보 유입. T0 이전 데이터로만 fit하고 transform만 적용

## 3-3. 누수 체크리스트

| 누수 경로 | 차단 방법 |
|---|---|
| 동시점/미래 매출 사용 | 개업일 이전 분기 데이터만 |
| 상권 집계에 자기 포함 | self-exclusion 후 재계산 |
| 클러스터링에 전체기간 사용 | train 기간으로만 fit |
| TargetEncoder | CV fold 내부에서만 fit (Pipeline에 넣기) |
| 스케일러/결측대치 | 반드시 Pipeline 내부 |
| 폐업 직전 상권 악화 신호 | 개업 시점 기준이므로 자동 차단 (방식 B의 장점) |

---

# 4. 모델링 — 2트랙 구조

## 트랙 1: 이진분류 (기준선 + 추천 엔진)

**타깃**: 개업 후 24개월 내 폐업 여부

```
Logistic Regression (해석 기준선)
   ↓
Random Forest (비선형 기준선)
   ↓
XGBoost + GridSearchCV (메인)
```

- 불균형 처리: `scale_pos_weight` 또는 `class_weight="balanced"` → **효과를 실제로 검증**하고 리포트
- 임계값 최적화: 단순 0.5가 아니라 비용 기반. "창업 실패 비용 vs 좋은 입지 놓치는 기회비용"으로 프레이밍
- **확률 보정(calibration) 필수**: 창업자에게 "폐업 확률 23%"를 보여줄 거라면 그 숫자가 실제 빈도와 맞아야 합니다. Calibration curve + Brier score로 검증하고, 필요시 isotonic/Platt 보정

## 트랙 2: 생존분석 (차별화 핵심)

**Kaplan-Meier**
- 상권 유형별 / 업종별 / 프랜차이즈 여부별 생존곡선 비교
- Log-rank test로 곡선 간 차이 유의성 검정
- 시각적 설득력이 가장 높은 산출물

**Cox Proportional Hazards**
- Hazard ratio 해석: "반경 300m 내 동일업종이 1개 늘면 폐업 위험 1.0X배"
- **비례위험 가정 검정 필수** (Schoenfeld residuals). 위반 시 → 시간가변 계수 또는 층화 Cox
- 이 검정을 하고 결과를 리포트하는 것만으로도 대부분의 포트폴리오와 차별화됩니다

**확장 (선택)**
- Random Survival Forest 또는 XGBoost survival objective → 비선형 + 생존 동시
- 두 트랙 결과가 일치하는지 교차 확인 (예: SHAP 상위 피처 vs Cox 유의 변수)

## 트랙 간 연결

이진분류와 생존분석이 **같은 코호트, 같은 피처**를 쓰므로 "이진분류는 무엇을 놓치는가"를 직접 보여줄 수 있습니다. 이게 프로젝트의 논리적 클라이맥스가 됩니다.

---

# 5. 평가 프로토콜

## 5-1. 데이터 분할 — 랜덤 split 금지

```
Train: 2019Q1 ~ 2020Q4 개업 코호트
Test:  2021Q1 ~ 2021Q4 개업 코호트
```

**시간 기반 분할**을 쓰는 이유: 실제 사용 시나리오가 "과거로 학습해 미래 창업을 예측"이기 때문. 랜덤 split은 같은 상권·같은 시기 점포가 train/test에 섞여 성능을 과대평가합니다.

추가로 **공간 기반 검증**도 고려해볼 만합니다: 특정 자치구를 통째로 held-out → "학습에 없던 지역에도 일반화되는가". 이건 지리 데이터에서 매우 설득력 있는 검증이고, 흔치 않습니다.

## 5-2. 지표

| 트랙 | 주 지표 | 보조 |
|---|---|---|
| 이진분류 | **AUC-PR** (불균형이므로) | AUC-ROC, Recall@k, F1 |
| 확률 품질 | **Brier score, Calibration curve** | — |
| 생존분석 | **Concordance index (C-index)** | Time-dependent AUC, Integrated Brier Score |
| 추천 래퍼 | 백테스트 생존율 격차 | (5-3 참조) |

## 5-3. 추천 래퍼의 백테스트 설계 ★

추천은 정답이 없으므로 **과거 재현 실험**으로 방어합니다.

```
1. 2021Q1 시점에 서 있다고 가정
2. 모델(2020Q4까지 학습)이 업종별 상권 순위를 산출
3. 실제 2021년에 그 상권들에서 개업한 점포들의 24개월 생존율 확인
4. 추천 상위 20% 상권 vs 하위 20% 상권의 실제 생존율 격차를 측정
```

이 격차가 유의하게 크면 → **"추천이 실제로 작동했다"는 정량적 근거**가 됩니다. 여기에 신뢰구간까지 붙이면 면접에서 강력한 카드가 됩니다.

주의: 상권 선택 자체가 비랜덤(좋은 상권엔 준비된 창업자가 몰림)이므로, **인과가 아니라 예측 성능임을 명시**하세요. 이 한계를 스스로 언급하는 것이 오히려 신뢰를 높입니다.

---

# 6. 해석 레이어

## 6-1. SHAP
- **Global (beeswarm)**: 전체적으로 무엇이 생존을 좌우하는가
- **Local (waterfall)**: "이 후보 입지가 왜 위험한가" → 추천 UI에 직접 연결
- **Dependence plot**: 경쟁밀도-생존 관계가 선형인지 역U자인지
- **업종별 분리 분석**: 요식업과 소매업의 위험 요인 차이 → 가장 흥미로운 인사이트가 나올 지점

## 6-2. PDP / ICE
- 경쟁밀도의 최적 구간이 존재하는가? (집적경제 vs 과당경쟁의 균형점)
- 이게 실재한다면 **"카페는 반경 300m 내 동종 N개까지는 오히려 유리"** 같은 반직관적 결론이 나옵니다. 프로젝트의 시그니처 인사이트가 될 수 있는 부분

## 6-3. 반사실적 설명 (선택)
DiCE 등으로 "이 입지에서 생존하려면 무엇이 달라져야 하는가". 여력이 있을 때만 — 우선순위는 낮습니다.

---

# 7. 프랜차이즈 확장 축 ★신규

앞선 조사 결과, 개별 매장 매출은 비공개지만 **위치·개업일·폐업일은 상호명 매칭으로 확보 가능**합니다. 이걸 별도 프로젝트가 아니라 **A의 피처이자 분석 축**으로 흡수합니다.

## 7-1. 브랜드 식별 파이프라인

```
상호명 원문 → 정규화(공백/특수문자/지점명 제거)
           → 정규식 1차 매칭 (주요 브랜드 사전)
           → 퍼지매칭(rapidfuzz) 2차
           → 수기 샘플 검증 (100~200건)
           → 매칭 정확도 리포트 (precision/recall)
```

**매칭 정확도를 측정해서 보고하는 것 자체가 데이터 품질 관리 역량의 증거**입니다. 대충 넘기지 말고 하나의 산출물로 만드세요.

## 7-2. 이 축에서 나오는 질문

- 프랜차이즈 점포는 개인 점포보다 실제로 오래 생존하는가? (KM 곡선 비교 + log-rank)
- 그 격차는 상권 유형에 따라 달라지는가? (교호작용)
- 브랜드별 생존곡선 차이 — 공정위 공시 데이터(브랜드 평균매출·폐점률)와 내 추정치가 일치하는가? → **외부 검증(external validation)**
- 프랜차이즈는 입지 선정을 더 잘 하는가, 아니면 브랜드 파워로 나쁜 입지를 버티는가? (피처 통제 전후 비교)

마지막 질문은 특히 좋습니다. 상권 피처를 통제했을 때 프랜차이즈 효과가 줄어들면 "입지 선정을 잘한 것", 유지되면 "브랜드 자체 효과". 이런 식으로 결론이 갈리는 분석은 스토리가 살아납니다.

---

# 8. 산출물 구성

| 산출물 | 내용 | 우선순위 |
|---|---|---|
| 전처리 파이프라인 | 재현 가능한 스크립트 + 캐싱 | 필수 |
| EDA 노트북 | 상권 유형 분포, 생존율 기초통계 | 필수 |
| 클러스터링 노트북 | KMeans/DBSCAN/PCA + 유형 해석 | 필수 |
| 예측 모델 노트북 | 이진분류 + 생존분석 2트랙 | 필수 |
| 해석 노트북 | SHAP/PDP/KM 곡선 | 필수 |
| **지도 시각화** | Folium/Kepler로 상권별 위험도 | 강력 권장 |
| Streamlit 대시보드 | 업종·예산 입력 → 추천 + SHAP 설명 | 권장 |
| README | 문제정의·설계결정·한계 정리 | **필수** |

> README에 "왜 개업 코호트를 선택했는가", "왜 시간 기반 split인가" 같은 **설계 결정의 근거**를 남기세요. 코드보다 이 문서가 실력을 더 잘 보여줍니다.

---

# 9. 실행 순서

## Phase 1 — 기반 (가장 오래 걸림)
1. LOCALDATA 인허가 수집 → 개업/폐업 이력 테이블 구축
2. 상권 SHP + CRS 통일 → 공간조인 검증 (지도 육안 확인)
3. **코호트 정의 확정** + 라벨 생성 → 폐업률 기초통계 확인
4. 상권분석서비스 5종 병합

## Phase 2 — 탐색
5. EDA: 업종별·상권별 생존율, 결측/이상치
6. 클러스터링 (PCA → KMeans/DBSCAN) → 상권 유형 명명

## Phase 3 — 모델링
7. 피처 엔지니어링 + 누수 체크리스트 통과
8. 이진분류 트랙 (LR → RF → XGBoost) + calibration
9. 생존분석 트랙 (KM → Cox → 비례위험 검정)

## Phase 4 — 해석·응용
10. SHAP/PDP 해석 + 업종별 비교
11. 프랜차이즈 축 분석
12. 추천 로직 + **백테스트**
13. 지도 시각화 + Streamlit + README

**Phase 1이 전체 시간의 절반을 먹습니다.** 여기를 서두르면 Phase 3에서 전부 되돌아와야 하니 각오하고 시작하세요.

---

# 10. 리스크

| 리스크 | 심각도 | 대응 |
|---|---|---|
| **인허가 상태코드 해석 오류** | 치명 | 코드 체계 문서 확인 + 샘플 수기 검증 |
| **양도·양수가 폐업으로 기록** | 높음 | 대표자 변경 필드 확인. 불가시 한계로 명시 |
| CRS 불일치 | 높음 | 공간조인 후 지도 육안 검증 필수 |
| 2024년 표준단위구역 변경 | 중간 | 코호트를 변경 이전 기간으로 한정하면 회피 가능 |
| 상호명 매칭 오류 | 중간 | 정확도 측정 후 리포트, 임계값 조정 |
| 상권 폴리곤 미포함 점포 | 중간 | 비율 보고 + 처리 방식 문서화 |
| 코로나 충격 | 중간 | 개업 분기 더미로 통제, 민감도 분석 |
| API 호출 한도 | 낮음 | sleep, 배치, 로컬 캐싱 |
| 임대료 데이터 희소 | 낮음 | 없으면 과감히 제외. 억지로 넣지 말 것 |

---

# 11. 프로젝트의 한 줄 방어 논리

> "서울 신규 개업 점포 코호트를 구축하고, 개업 시점에 관측 가능했던 상권 특성만으로 24개월 생존을 예측했다. 이진분류(AUC-PR)와 생존분석(C-index) 두 트랙으로 접근했고, 시간 기반 분할과 과거 재현 백테스트로 추천 로직의 실효성을 검증했다."

이 문장을 자신 있게 말할 수 있으면 프로젝트는 성공한 겁니다.

---

*본 문서의 API 명세 및 데이터셋 구조는 확인 시점 기준이며, 실제 사용 전 각 제공처에서 최신 스펙을 확인하시기 바랍니다. 특히 인허가 상태코드 체계와 상권 공간단위 정의는 프로젝트 초반에 반드시 원문 확인이 필요합니다.*
